package tryabstractinterface;

public interface CC extends DD { // implements must be interface
  // interface extends interface, (XX not implements interface)

  int VOLUME = 5; // Constants, implicitly static final
  String cccc = "c";
  static Long ccccc = 5L;

void m1();
void m2();


}
