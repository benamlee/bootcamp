package fruit;

class Apple { // no public => package private
    public static void main(String[] args) {
        // cannot reference to Cat.java (package-private), because it is under another package
        // Cat Cat = new Cat();
    
      }
}
